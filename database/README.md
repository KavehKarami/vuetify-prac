#### for Run `db.json`, follow these steps:
- install `json-server` globally in your os: `npm i -g json-server`
- open terminal in `database` folder and run this command: `json-server db.json -p 5000 -d 500 --watch`